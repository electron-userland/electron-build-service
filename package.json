{
  "private": true,
  "license": "MIT",
  "scripts": {
    "dist": "yarn build && yarn build-router && yarn push && yarn push-router && yarn bundle"
  },
  "dependencies": {
    "app-builder-lib": "22.5.1"
  }
}
